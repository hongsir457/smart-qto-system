# AI处理核心依赖（简化版，避免版本冲突）
paddlepaddle==2.5.1
paddleocr==2.7.0.3
# opencv-python会被paddleocr自动安装合适版本
Pillow==10.0.0
numpy==1.24.3

# OpenAI GPT集成
openai==1.3.5

# 数据处理
pandas==2.0.3
openpyxl==3.1.2

# 注意：torch、torchvision、ultralytics暂时移除以避免依赖冲突
# 后续需要YOLOv8时可单独安装 